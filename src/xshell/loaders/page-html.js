import Page from "../page.js"

//export 
export default class LoaderPageHtml {
    async load(src) {
        const request = await fetch(src);
        debugger;
        
        
        return null;
    }
};